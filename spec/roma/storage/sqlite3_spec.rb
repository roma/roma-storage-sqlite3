RSpec.describe Roma::Storage::Sqlite3 do
  it "has a version number" do
    expect(Roma::Storage::Sqlite3::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
